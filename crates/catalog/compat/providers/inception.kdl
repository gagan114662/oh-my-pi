// Provider-wire compat for "inception"; regenerated from the frozen census using deployment contract selectors.

provider "inception" {
	// residue: taxonomy ranks and exact globs do not isolate these models.
	models "mercury-2" {
		thinking-efforts "minimal" "low" "medium" "high" "xhigh"
		thinking-mode "effort"
	}
}
