# omp-secrets

`omp-secrets` owns the closed secret-rule contract and allocation-conscious primitives used to mask provider-bound text and restore keyed placeholders. Secret-key persistence lives in `omp-cache` and process policy stays in `omp-app`; this crate is deterministic and side-effect free.
